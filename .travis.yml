language: python
sudo: false  # Speed up build, allegedly.
python:
  - "3.6"
install:
  - pip install pytest
  - pip install pytest-cov
  - pip install python-coveralls
# command to run tests
script:
  - pytest --cov=tregex test/
# Push the results back to codecov
after_success:
  - coveralls